@echo off
# 默认关闭回显

@windows {
    echo "Windows环境下执行，回显已关闭"
    echo "你看不到执行的命令"
}

@unix {
    echo "Unix环境下执行，回显已关闭"
    echo "你看不到执行的命令"
}

@echo on
# 现在打开回显

@windows {
    echo "Windows环境下执行，回显已打开"
    echo "现在你能看到执行的命令了"
}

@default {
    echo "默认环境下的执行"
    echo "这条命令会在所有未指定的平台上执行"
}