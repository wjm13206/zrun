@windows {
    echo "Running on Windows"
    dir
}

@unix {
    echo "Running on Unix-like system"
    ls -la
}

@linux {
    echo "Linux specific command"
    cat /proc/version
}

@default {
    echo "This runs on any unspecified platform"
    echo "Current OS:" 
    uname -a
}